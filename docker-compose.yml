version: '3.8'

services:
  db:
    image: postgres:15-alpine # 18.3 yerine stabil 15 veya 16 öneririm (18 çok yeni/beta olabilir)
    container_name: sahad_db
    restart: always
    environment:
      POSTGRES_USER: sahad_user
      POSTGRES_PASSWORD: sahad_password
      POSTGRES_DB: sahad_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./backend
    container_name: sahad_backend
    restart: always
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "postgresql://sahad_user:sahad_password@db:5432/sahad_db"
      PORT: 3000
    depends_on:
      - db

  frontend:
    build: ./frontend
    container_name: sahad_frontend
    restart: always
    ports:
      - "3001:3000" # Dışarıdan 3001 ile erişilecek, içeride 3000'de çalışıyor
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:3000
    depends_on:
      - backend

volumes:
  postgres_data: